export default function reducer(state={
    client: {},
    fetching: false,
    fetched: false,
    error: null
}, action) {
    let newState = {}
    switch(action.type) {
        case "": {

        }
    }
    return newState
};