export default function reducer(state={
    settings: {},
    fetching: false,
    fetched: false,
    error: null
}, action) {
    let newState = {}
    switch(action.type) {
        case "": {

        }
    }
    return newState
};