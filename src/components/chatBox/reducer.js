export default function reducer(state={
    settings: {
    },
    fetching: false,
    fetched: false,
    error: null
}, action) {
    switch(action.type) {
      
    }
    return state
};